
<%= render "shared/context_nav", title: t("activerecord.models.#{Titulo.model_name.i18n_key}.other") do  %>
<span class="breadcrumb-item"><%= link_to t("activerecord.models.#{Titulo.model_name.i18n_key}.other"), titulos_path, class: "text-dark" %></span>
<span class="breadcrumb-item active"><%= t("helpers.submit.#{params[:action]}") %></span>
<% end %>
<%= simple_form_for(@titulo) do |f| %>
<div class="card">
  <div class="card-header">
    <h5 class="my-0">
      <%= @titulo.new_record? ? "Incluir " : "Editar " %><%= t("activerecord.models.#{Titulo.model_name.i18n_key}.one") %>
    </h5>
  </div>
  <div class="card-body">
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
    <div class="row">
      
        
          <div class="col-12">
            <%= f.input :conteudo_text, input_html: { class: "capitalize"}, placeholder: "Conteudo text"  %>
          </div>
        
        
          <div class="col-12">
            <%= f.input :data_evento, input_html: { class: "capitalize"}, placeholder: "Data evento"  %>
          </div>
        
        
          <div class="col-12">
            <%= f.association :l_categoria, input_html: { class: "capitalize"}, placeholder: "L categoria"  %>
          </div>
        
    </div>
  </div>
  <div class="card-footer">
    <%= btn_submit(f) %>
    <%= link_to "Voltar", titulos_path, class: "btn btn-light" %>
  </div>
</div>
<% end %>